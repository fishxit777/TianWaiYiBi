# V33 五面向體驗整理實作計畫

範圍已由使用者確認：前台、後台、分頁、WEB-APP 現有登入／已購取回／閱讀、手機模式，各列十項可重現問題及修復；不新增安裝式 App。

## 驗收標準

1. 五類各十項，以來源或瀏覽器證據記錄問題、改法及驗證，共用原因明確標示，不湊不存在的缺點。
2. 桌機、平板、390px、360px 無頁面橫向溢出、文字／按鈕／仙伴對話遮擋；導覽、表單、FAQ 正常操作。
3. 不改價格、不開公開收款、不復活留言；測試、機密檢查、文件、commit、push、正式健康及同步核對完成才能交付。

## 已核對基線

- main/origin/main 同步、工作樹乾淨；HEAD e642160。
- 正式 /healthz：status=ok、release=independent-content-security-v32。
- 已完整讀取 HANDOFF.md 與指定的 V19、封印市場、PostgreSQL/Passkey 交接。

## 工作分工與執行順序

1. 後台：四個 admin 模板、static/admin.js、獨立 static/v33-admin.css；先保留全部既有欄位／安全檢查，再修標籤、狀態、時間、表格及手機登出。tests/test_ux_admin_v33.py。
2. 分頁：faq/policies/support/idea_detail 模板、static/v33-pages.css。政策實質條文不改，以雜湊對照；修導覽、閱讀寬度、資訊分節。tests/test_ux_pages_v33.py。
3. 已購流程：既有八個流程模板、static/v33-flows.css、static/flows.js；不改付費權限與後端，補表單狀態、錯誤、頁內閱讀、授權大圖。tests/test_ux_flows_v33.py。
4. 主整合：base/home 模板、app.js、companions.js、static/v33.css；完整手機導覽、分類連結與歷史、互動回饋、可讀性與觸控。tests/test_ux_shared_v33.py。
5. 使用 _local/qa-v33/ 拋棄式 loopback 合成資料庫；外部金流、郵件、LINE 憑證不載入，無正式異動。
6. 三輪內對照驗收；修復不通過項再測；全套 pytest、JS 語法、git diff --check、僅回報規則及檔名的機密檢查。
7. 更新 HANDOFF 與50項驗收總表，提交推送，等待正式版本及公開頁檢查，最後核對 main/origin/main。

## 不可突破的邊界

不執行正式付款、退款、Passkey、備份還原或攻擊試驗；不更動 LINE 排程/隊列。不顯示個人資料、信箱、訂單識別碼、私密圖文或憑證。不使用其他專案或舊對話作為程式來源。

## 驗證紀錄位置

docs/updates/2026-09-13-ux-{admin,pages,flows}-v33.md 與主驗收報告。若正式證據不足，須明確標示未通過，不以本機結果代替正式驗收。
